node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example


__pycache__/
qdrant_data/
qdrant_storage/
*.sqlite
*.sqlite3
*.zip
*.parquet
venv/
phase1/
phase2/
# --- index build artifacts (ship alongside the snapshot, never via git) ---
*.sqlite-wal
*.sqlite-shm
index_manifest.json
snapshots/
*.snapshot
qdrant_bin/
*.pre-migration.bak

# --- benchmark / calibration output ---
before.json
after.json
after-tuning.json
compare.json
groq.json
sarvam.json
calib.json
gates.json
results.json

# --- agent / editor tooling ---
# Local dev-server launch config used for previewing the frontend. Tooling
# state for whoever is at the keyboard, not project configuration.
.claude/

# --- local working notes ---
# Operational handoff notes: live env values, measurements, open defects.
# Deliberately never committed -- this is a scratch record for whoever is at
# the keyboard, not part of the project.
NOTES.md

# --- locally generated query sets ---
# Sampled/curated MSMARCO-XI queries used for ad-hoc evaluation. Regenerated
# from parents.sqlite when needed, so they are working data rather than
# project inputs.
backend/sampled_50_queries.json
backend/curated_50_questions.json

# local voice recordings used with backend/probe_realtime_stt.py
*.wav
